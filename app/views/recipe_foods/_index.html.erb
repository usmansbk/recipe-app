<h1>Recipe Foods</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% recipe_foods.each do |recipe_food| %>
      <tr>
        <td><%= link_to 'Edit', edit_recipe_food_path(id: recipe_food.food_id) %></td>
        <td><%= link_to 'Destroy', recipe_food_path(id: recipe_food.food_id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
